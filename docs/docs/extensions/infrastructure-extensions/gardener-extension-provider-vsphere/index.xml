<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Gardener – Provider vSphere</title><link>https://gardener.cloud/docs/extensions/infrastructure-extensions/gardener-extension-provider-vsphere/</link><description>Recent content in Provider vSphere on Gardener</description><generator>Hugo -- gohugo.io</generator><language>en-US</language><atom:link href="https://gardener.cloud/docs/extensions/infrastructure-extensions/gardener-extension-provider-vsphere/index.xml" rel="self" type="application/rss+xml"/><item><title>Docs: Deployment</title><link>https://gardener.cloud/docs/extensions/infrastructure-extensions/gardener-extension-provider-vsphere/docs/deployment/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://gardener.cloud/docs/extensions/infrastructure-extensions/gardener-extension-provider-vsphere/docs/deployment/</guid><description>
&lt;h1 id="deployment-of-the-vsphere-provider-extension">Deployment of the vSphere provider extension&lt;/h1>
&lt;p>&lt;strong>Disclaimer:&lt;/strong> This document is NOT a step by step installation guide for the vSphere provider extension and only contains some configuration specifics regarding the installation of different components via the helm charts residing in the vSphere provider extension &lt;a href="https://github.com/gardener/gardener-extension-provider-vsphere">repository&lt;/a>.&lt;/p>
&lt;h2 id="gardener-extension-validator-vsphere">gardener-extension-validator-vsphere&lt;/h2>
&lt;h3 id="authentication-against-the-garden-cluster">Authentication against the Garden cluster&lt;/h3>
&lt;p>There are several authentication possibilities depending on whether or not &lt;a href="https://github.com/gardener/garden-setup#concept-the-virtual-cluster">the concept of &lt;em>Virtual Garden&lt;/em>&lt;/a> is used.&lt;/p>
&lt;h4 id="virtual-garden-is-not-used-ie-the-runtime-garden-cluster-is-also-the-target-garden-cluster">&lt;em>Virtual Garden&lt;/em> is not used, i.e., the &lt;code>runtime&lt;/code> Garden cluster is also the &lt;code>target&lt;/code> Garden cluster.&lt;/h4>
&lt;p>&lt;strong>Automounted Service Account Token&lt;/strong>
The easiest way to deploy the &lt;code>gardener-extension-validator-vsphere&lt;/code> component will be to not provide &lt;code>kubeconfig&lt;/code> at all. This way in-cluster configuration and an automounted service account token will be used. The drawback of this approach is that the automounted token will not be automatically rotated.&lt;/p>
&lt;p>&lt;strong>Service Account Token Volume Projection&lt;/strong>
Another solution will be to use &lt;a href="https://kubernetes.io/docs/tasks/configure-pod-container/configure-service-account/#service-account-token-volume-projection">Service Account Token Volume Projection&lt;/a> combined with a &lt;code>kubeconfig&lt;/code> referencing a token file (see example below).&lt;/p>
&lt;div class="highlight">&lt;pre style="background-color:#fff;-moz-tab-size:4;-o-tab-size:4;tab-size:4">&lt;code class="language-yaml" data-lang="yaml">apiVersion: v1
kind: Config
clusters:
- cluster:
certificate-authority-data: &amp;lt;CA-DATA&amp;gt;
server: https://default.kubernetes.svc.cluster.local
name: garden
contexts:
- context:
cluster: garden
user: garden
name: garden
current-context: garden
users:
- name: garden
user:
tokenFile: /var/run/secrets/projected/serviceaccount/token
&lt;/code>&lt;/pre>&lt;/div>&lt;p>This will allow for automatic rotation of the service account token by the &lt;code>kubelet&lt;/code>. The configuration can be achieved by setting both &lt;code>.Values.global.serviceAccountTokenVolumeProjection.enabled: true&lt;/code> and &lt;code>.Values.global.kubeconfig&lt;/code> in the respective chart&amp;rsquo;s &lt;code>values.yaml&lt;/code> file.&lt;/p>
&lt;h4 id="virtual-garden-is-used-ie-the-runtime-garden-cluster-is-different-from-the-target-garden-cluster">&lt;em>Virtual Garden&lt;/em> is used, i.e., the &lt;code>runtime&lt;/code> Garden cluster is different from the &lt;code>target&lt;/code> Garden cluster.&lt;/h4>
&lt;p>&lt;strong>Service Account&lt;/strong>
The easiest way to setup the authentication will be to create a service account and the respective roles will be bound to this service account in the &lt;code>target&lt;/code> cluster. Then use the generated service account token and craft a &lt;code>kubeconfig&lt;/code> which will be used by the workload in the &lt;code>runtime&lt;/code> cluster. This approach does not provide a solution for the rotation of the service account token. However, this setup can be achieved by setting &lt;code>.Values.global.virtualGarden.enabled: true&lt;/code> and following these steps:&lt;/p>
&lt;ol>
&lt;li>Deploy the &lt;code>application&lt;/code> part of the charts in the &lt;code>target&lt;/code> cluster.&lt;/li>
&lt;li>Get the service account token and craft the &lt;code>kubeconfig&lt;/code>.&lt;/li>
&lt;li>Set the crafted &lt;code>kubeconfig&lt;/code> and deploy the &lt;code>runtime&lt;/code> part of the charts in the &lt;code>runtime&lt;/code> cluster.&lt;/li>
&lt;/ol>
&lt;p>&lt;strong>Client Certificate&lt;/strong>
Another solution will be to bind the roles in the &lt;code>target&lt;/code> cluster to a &lt;code>User&lt;/code> subject instead of a service account and use a client certificate for authentication. This approach does not provide a solution for the client certificate rotation. However, this setup can be achieved by setting both &lt;code>.Values.global.virtualGarden.enabled: true&lt;/code> and &lt;code>.Values.global.virtualGarden.user.name&lt;/code>, then following these steps:&lt;/p>
&lt;ol>
&lt;li>Generate a client certificate for the &lt;code>target&lt;/code> cluster for the respective user.&lt;/li>
&lt;li>Deploy the &lt;code>application&lt;/code> part of the charts in the &lt;code>target&lt;/code> cluster.&lt;/li>
&lt;li>Craft a &lt;code>kubeconfig&lt;/code> using the already generated client certificate.&lt;/li>
&lt;li>Set the crafted &lt;code>kubeconfig&lt;/code> and deploy the &lt;code>runtime&lt;/code> part of the charts in the &lt;code>runtime&lt;/code> cluster.&lt;/li>
&lt;/ol>
&lt;p>&lt;strong>Projected Service Account Token&lt;/strong>
This approach requires an already deployed and configured &lt;a href="https://github.com/gardener/oidc-webhook-authenticator">oidc-webhook-authenticator&lt;/a> for the &lt;code>target&lt;/code> cluster. Also the &lt;code>runtime&lt;/code> cluster should be registered as a trusted identity provider in the &lt;code>target&lt;/code> cluster. Then projected service accounts tokens from the &lt;code>runtime&lt;/code> cluster can be used to authenticate against the &lt;code>target&lt;/code> cluster. The needed steps are as follows:&lt;/p>
&lt;ol>
&lt;li>Deploy &lt;a href="https://github.com/gardener/oidc-webhook-authenticator">OWA&lt;/a> and establish the needed trust.&lt;/li>
&lt;li>Set &lt;code>.Values.global.virtualGarden.enabled: true&lt;/code> and &lt;code>.Values.global.virtualGarden.user.name&lt;/code>. &lt;strong>Note:&lt;/strong> username value will depend on the trust configuration, e.g., &lt;code>&amp;lt;prefix&amp;gt;:system:serviceaccount:&amp;lt;namespace&amp;gt;:&amp;lt;serviceaccount&amp;gt;&lt;/code>&lt;/li>
&lt;li>Set &lt;code>.Values.global.serviceAccountTokenVolumeProjection.enabled: true&lt;/code> and &lt;code>.Values.global.serviceAccountTokenVolumeProjection.audience&lt;/code>. &lt;strong>Note:&lt;/strong> audience value will depend on the trust configuration, e.g., &lt;code>&amp;lt;cliend-id-from-trust-config&amp;gt;&lt;/code>.&lt;/li>
&lt;li>Craft a kubeconfig (see example below).&lt;/li>
&lt;li>Deploy the &lt;code>application&lt;/code> part of the charts in the &lt;code>target&lt;/code> cluster.&lt;/li>
&lt;li>Deploy the &lt;code>runtime&lt;/code> part of the charts in the &lt;code>runtime&lt;/code> cluster.&lt;/li>
&lt;/ol>
&lt;div class="highlight">&lt;pre style="background-color:#fff;-moz-tab-size:4;-o-tab-size:4;tab-size:4">&lt;code class="language-yaml" data-lang="yaml">apiVersion: v1
kind: Config
clusters:
- cluster:
certificate-authority-data: &amp;lt;CA-DATA&amp;gt;
server: https://virtual-garden.api
name: virtual-garden
contexts:
- context:
cluster: virtual-garden
user: virtual-garden
name: virtual-garden
current-context: virtual-garden
users:
- name: virtual-garden
user:
tokenFile: /var/run/secrets/projected/serviceaccount/token
&lt;/code>&lt;/pre>&lt;/div></description></item><item><title>Docs: Local Setup</title><link>https://gardener.cloud/docs/extensions/infrastructure-extensions/gardener-extension-provider-vsphere/docs/local-setup/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://gardener.cloud/docs/extensions/infrastructure-extensions/gardener-extension-provider-vsphere/docs/local-setup/</guid><description>
&lt;h3 id="admission-vsphere">admission-vsphere&lt;/h3>
&lt;p>&lt;code>admission-vsphere&lt;/code> is an admission webhook server which is responsible for the validation of the cloud provider (vSphere in this case) specific fields and resources. The Gardener API server is cloud provider agnostic and it wouldn&amp;rsquo;t be able to perform similar validation.&lt;/p>
&lt;p>Follow the steps below to run the admission webhook server locally.&lt;/p>
&lt;ol>
&lt;li>
&lt;p>Start the Gardener API server.&lt;/p>
&lt;p>For details, check the Gardener &lt;a href="https://gardener.cloud/docs/gardener/development/local_setup/">local setup&lt;/a>.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>Start the webhook server&lt;/p>
&lt;p>Make sure that the &lt;code>KUBECONFIG&lt;/code> environment variable is pointing to the local garden cluster.&lt;/p>
&lt;div class="highlight">&lt;pre style="background-color:#fff;-moz-tab-size:4;-o-tab-size:4;tab-size:4">&lt;code class="language-bash" data-lang="bash">make start-admission
&lt;/code>&lt;/pre>&lt;/div>&lt;/li>
&lt;li>
&lt;p>Setup the &lt;code>ValidatingWebhookConfiguration&lt;/code>.&lt;/p>
&lt;p>&lt;code>hack/dev-setup-admission-vsphere.sh&lt;/code> will configure the webhook Service which will allow the kube-apiserver of your local cluster to reach the webhook server. It will also apply the &lt;code>ValidatingWebhookConfiguration&lt;/code> manifest.&lt;/p>
&lt;div class="highlight">&lt;pre style="background-color:#fff;-moz-tab-size:4;-o-tab-size:4;tab-size:4">&lt;code class="language-bash" data-lang="bash">./hack/dev-setup-admission-vsphere.sh
&lt;/code>&lt;/pre>&lt;/div>&lt;/li>
&lt;/ol>
&lt;p>You are now ready to experiment with the &lt;code>admission-vsphere&lt;/code> webhook server locally.&lt;/p></description></item><item><title>Docs: Prepare Vsphere</title><link>https://gardener.cloud/docs/extensions/infrastructure-extensions/gardener-extension-provider-vsphere/docs/prepare-vsphere/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://gardener.cloud/docs/extensions/infrastructure-extensions/gardener-extension-provider-vsphere/docs/prepare-vsphere/</guid><description>
&lt;h1 id="vsphere--nsx-t-preparation-for-gardener-extension-vsphere-provider">vSphere / NSX-T Preparation for Gardener Extension &amp;ldquo;vSphere Provider&amp;rdquo;&lt;/h1>
&lt;ul>
&lt;li>&lt;a href="#vsphere--nsx-t-preparation-for-gardener-extension-vsphere-provider">vSphere / NSX-T Preparation for Gardener Extension &amp;ldquo;vSphere Provider&amp;rdquo;&lt;/a>
&lt;ul>
&lt;li>&lt;a href="#vsphere-preparation">vSphere Preparation&lt;/a>
&lt;ul>
&lt;li>&lt;a href="#create-folders">Create Folders&lt;/a>&lt;/li>
&lt;li>&lt;a href="#upload-vm-templates-for-worker-nodes">Upload VM Templates for Worker Nodes&lt;/a>&lt;/li>
&lt;li>&lt;a href="#prepare-for-kubernetes-zones-and-regions">Prepare for Kubernetes Zones and Regions&lt;/a>
&lt;ul>
&lt;li>&lt;a href="#create-resource-pools">Create Resource Pool(s)&lt;/a>&lt;/li>
&lt;li>&lt;a href="#tag-regions-and-zones">Tag Regions and Zones&lt;/a>&lt;/li>
&lt;li>&lt;a href="#storage-policies">Storage policies&lt;/a>
&lt;ul>
&lt;li>&lt;a href="#tag-zone-storages">Tag Zone Storages&lt;/a>&lt;/li>
&lt;li>&lt;a href="#create-or-clone-vm-storage-policy">Create or clone VM Storage Policy&lt;/a>&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;a href="#nsx-t-prepartion">NSX-T Prepartion&lt;/a>
&lt;ul>
&lt;li>&lt;a href="#create-ip-pools">Create IP pools&lt;/a>
&lt;ul>
&lt;li>&lt;a href="#sizing-the-ip-pools">Sizing the IP pools&lt;/a>&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;a href="#check-edge-cluster-sizing">Check edge cluster sizing&lt;/a>&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;a href="#get-vds-uuids">Get VDS UUIDs&lt;/a>&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ul>
&lt;p>Several preparational steps are necessary for VMware vSphere and NSX-T, before this extension can be used
to create Gardener shoot clusters.&lt;/p>
&lt;p>The main version target of this extension is vSphere 7.x together with NSX-T 3.x.
The recommended environment is a system setup with VMware Cloud Foundation (VCF) 4.1.
Older versions like vSphere 6.7U3 with NSX-T 2.5 or 3.0 should still work, but are not tested extensively.&lt;/p>
&lt;h2 id="vsphere-preparation">vSphere Preparation&lt;/h2>
&lt;h3 id="create-folders">Create Folders&lt;/h3>
&lt;p>Two folders need to be created:
- a folder which will contain the VMs of the shoots (cloud profile &lt;code>spec.providerConfig.folder&lt;/code>)
- a folder containing templates (used by cloud profile &lt;code>spec.providerConfig.machineImages[*].versions[*].path&lt;/code>)&lt;/p>
&lt;p>In vSphere client:&lt;/p>
&lt;ol>
&lt;li>From the &lt;em>Menu&lt;/em> in the vSphere Client toolbar choose &lt;em>VMs and Templates&lt;/em>&lt;/li>
&lt;li>Select the vSphere Datacenter of the work load vCenter in the browser&lt;/li>
&lt;li>From the context menu select &lt;em>New Folder&lt;/em> &amp;gt; &lt;em>New VM and Template Folder&lt;/em>, set folder name to e.g. &amp;ldquo;gardener&amp;rdquo;&lt;/li>
&lt;li>From the context menu of the new folder &lt;em>gardener&lt;/em> select &lt;em>New Folder&lt;/em>, set folder name to &amp;ldquo;templates&amp;rdquo;&lt;/li>
&lt;/ol>
&lt;h3 id="upload-vm-templates-for-worker-nodes">Upload VM Templates for Worker Nodes&lt;/h3>
&lt;p>Upload gardenlinux OVA (or OVF/VMDK) templates.&lt;/p>
&lt;ol>
&lt;li>From the context menu of the folder &lt;code>gardener/templates&lt;/code> choose &lt;em>Deploy OVF Template&amp;hellip;&lt;/em>&lt;/li>
&lt;li>Adjust name if needed&lt;/li>
&lt;li>Select any compute cluster as compute resource&lt;/li>
&lt;li>Select a storage (e.g. VSAN)&lt;/li>
&lt;li>Select any network (not important)&lt;/li>
&lt;li>No need to customize the template&lt;/li>
&lt;li>After deployment is finished select from the context menu of the new deployed VM &lt;em>Template&lt;/em> &amp;gt; &lt;em>Convert To Template&lt;/em>&lt;/li>
&lt;/ol>
&lt;h3 id="prepare-for-kubernetes-zones-and-regions">Prepare for Kubernetes Zones and Regions&lt;/h3>
&lt;p>If the vSphere infrastructure is setup for multiple availabilities zones and Kubernetes should be topology aware, there need to be defined two labels in the cloud profile (section &lt;code>spec.providerConfig.failureDomainLabels&lt;/code>)&lt;/p>
&lt;div class="highlight">&lt;pre style="background-color:#fff;-moz-tab-size:4;-o-tab-size:4;tab-size:4">&lt;code class="language-yaml" data-lang="yaml"> failureDomainLabels:
region: k8s-region
zone: k8s-zone
&lt;/code>&lt;/pre>&lt;/div>&lt;p>See also: &lt;a href="https://vsphere-csi-driver.sigs.k8s.io/driver-deployment/deploying_csi_with_zones.html">deploying_csi_with_zones&lt;/a>&lt;/p>
&lt;p>A Kubernetes zone can either be a vCenter or one of its datacenters&lt;/p>
&lt;p>Zones must be subresources of it. If the region is a complete vCenter, the zone must specify datacenter and either compute cluster or resource pool.
Otherwise, i.e. tf the region is a datacenter, the zone must specify either compute cluster or resource pool.&lt;/p>
&lt;p>In the following steps it is assumed:
- the region is specified by a datacenter
- the zone is specified by a compute clusters or one of its resource pools&lt;/p>
&lt;h4 id="create-resource-pools">Create Resource Pool(s)&lt;/h4>
&lt;p>Create a resource pool for every zone:&lt;/p>
&lt;ol>
&lt;li>From the &lt;em>Menu&lt;/em> in the vSphere Client toolbar choose &lt;em>Hosts and Clusters&lt;/em>&lt;/li>
&lt;li>From the context menu of the compute cluster select &lt;em>New Resource Pool&amp;hellip;&lt;/em> and provide the name of the zone. CPU and Memory settings are optional.&lt;/li>
&lt;/ol>
&lt;h4 id="tag-regions-and-zones">Tag Regions and Zones&lt;/h4>
&lt;p>Eeach zone must be tagged with the category defined by the label defined in the cloud profile (&lt;code>spec.providerConfig.failureDomainLabels.region&lt;/code>).
Assuming that the region is a datacenter and the region label is &lt;code>k8s-region&lt;/code>:&lt;/p>
&lt;ol>
&lt;li>From the &lt;em>Menu&lt;/em> in the vSphere Client toolbar choose &lt;em>Hosts and Clusters&lt;/em>&lt;/li>
&lt;li>Select the region&amp;rsquo;s datacenter in the browser&lt;/li>
&lt;li>In the &lt;em>Summary&lt;/em> tab there is a subwindow titled &lt;em>Tags&lt;/em>. Click the &lt;em>Assign&amp;hellip;&lt;/em> link.&lt;/li>
&lt;li>In the &lt;em>Assign Tag&lt;/em> dialog select the &lt;em>ADD TAG&lt;/em> link above of the table&lt;/li>
&lt;li>In the &lt;em>Create Tag&lt;/em> dialog choose the &lt;em>k8s-region&lt;/em> category. If it is not defined, click the &lt;em>Create New Category&lt;/em> link to create the category.&lt;/li>
&lt;li>Enter the &lt;em>Name&lt;/em> of the region.&lt;/li>
&lt;li>Back in the &lt;em>Assign Tag&lt;/em> mark the checkbox of the region tag you just have created.&lt;/li>
&lt;li>Click the &lt;em>ASSIGN&lt;/em> button&lt;/li>
&lt;/ol>
&lt;p>Assuiming that the zone are specified by resource pools and the zone label is &lt;code>k8s-zone&lt;/code>:&lt;/p>
&lt;ol>
&lt;li>From the &lt;em>Menu&lt;/em> in the vSphere Client toolbar choose &lt;em>Hosts and Clusters&lt;/em>&lt;/li>
&lt;li>Select the zone&amp;rsquo;s compute cluster in the browser&lt;/li>
&lt;li>In the &lt;em>Summary&lt;/em> tab there is a subwindow titled &lt;em>Tags&lt;/em>. Click the &lt;em>Assign&amp;hellip;&lt;/em> link.&lt;/li>
&lt;li>In the &lt;em>Assign Tag&lt;/em> dialog select the &lt;em>ADD TAG&lt;/em> link above of the table&lt;/li>
&lt;li>In the &lt;em>Create Tag&lt;/em> dialog choose the &lt;em>k8s-zone&lt;/em> category. If it is not defined, click the &lt;em>Create New Category&lt;/em> link to create the category.&lt;/li>
&lt;li>Enter the &lt;em>Name&lt;/em> of the zone.&lt;/li>
&lt;li>Back in the &lt;em>Assign Tag&lt;/em> mark the checkbox of the zone tag you just have created.&lt;/li>
&lt;li>Click the &lt;em>ASSIGN&lt;/em> button&lt;/li>
&lt;/ol>
&lt;h4 id="storage-policies">Storage policies&lt;/h4>
&lt;p>Each zone can have a separate storage. In this case a storage policy is needed to be compatible with all the zone storages.&lt;/p>
&lt;h5 id="tag-zone-storages">Tag Zone Storages&lt;/h5>
&lt;p>For each zone tag the storage with the corresponding &lt;code>k8s-zone&lt;/code> tag for the zone.&lt;/p>
&lt;ol>
&lt;li>From the &lt;em>Menu&lt;/em> in the vSphere Client toolbar choose &lt;em>Storage&lt;/em>&lt;/li>
&lt;li>Select the zone&amp;rsquo;s storage in the browser&lt;/li>
&lt;li>In the &lt;em>Summary&lt;/em> tab there is a subwindow titled &lt;em>Tags&lt;/em>. Click the &lt;em>Assign&amp;hellip;&lt;/em> link.&lt;/li>
&lt;li>In the &lt;em>Assign Tag&lt;/em> dialog select the &lt;em>ADD TAG&lt;/em> link above of the table&lt;/li>
&lt;li>In the &lt;em>Create Tag&lt;/em> dialog choose the &lt;em>k8s-zone&lt;/em> category. If it is not defined, click the &lt;em>Create New Category&lt;/em> link to create the category.&lt;/li>
&lt;li>Enter the &lt;em>Name&lt;/em> of the zone.&lt;/li>
&lt;li>Back in the &lt;em>Assign Tag&lt;/em> mark the checkbox of the zone tag you just have created.&lt;/li>
&lt;li>Click the &lt;em>ASSIGN&lt;/em> button&lt;/li>
&lt;/ol>
&lt;h5 id="create-or-clone-vm-storage-policy">Create or clone VM Storage Policy&lt;/h5>
&lt;ol>
&lt;li>
&lt;p>From the &lt;em>Menu&lt;/em> in the vSphere Client toolbar choose &lt;em>Policies and Profiles&lt;/em>&lt;/p>
&lt;/li>
&lt;li>
&lt;p>In the &lt;em>Policiies and Profiles&lt;/em> list select &lt;em>VM Storage Policies&lt;/em>&lt;/p>
&lt;/li>
&lt;li>
&lt;p>Create or clone an exisitng storage policy&lt;/p>
&lt;p>a) set name, e.g. &amp;ldquo;&amp;lt;region-name&amp;gt; Storage Policy&amp;rdquo; (will be needed for the cloud profile later)&lt;/p>
&lt;p>b) On the page &lt;em>Policy structure&lt;/em> check only the checkbox &lt;em>Enable tag based placement rules&lt;/em>&lt;/p>
&lt;p>c) On the page &lt;em>Tage based placement&lt;/em> press the &lt;em>ADD TAG RULE&lt;/em> button.&lt;/p>
&lt;p>d) For &lt;em>Rule 1&lt;/em> select&lt;/p>
&lt;pre>&lt;code>*Tag category* = *k8s-zone*
*Usage option* = *Use storage tagged with*
*Tags* = *all zone tags*.
&lt;/code>&lt;/pre>&lt;p>e) Validate the compatible storages on the page &lt;em>Storage compatibility&lt;/em>&lt;/p>
&lt;p>f) Press &lt;em>FINISH&lt;/em> on the &lt;em>Review and finish&lt;/em> page&lt;/p>
&lt;/li>
&lt;/ol>
&lt;h2 id="nsx-t-prepartion">NSX-T Prepartion&lt;/h2>
&lt;p>A shared NSX-T is needed for all zones of a region.
External IP address ranges are needed for SNAT and load balancers.
Besides the edge cluster must sized large enough to deal with the load balancers of all shoots.&lt;/p>
&lt;h3 id="create-ip-pools">Create IP pools&lt;/h3>
&lt;p>Two IP pools are needed for external IP addresses.&lt;/p>
&lt;ol>
&lt;li>IP pool for &lt;strong>SNAT&lt;/strong>
The IP pool name needs to be specified in the cloud profile at &lt;code>spec.providerConfig.regions[*].snatIPPool&lt;/code>. Each shoot cluster needs one SNAT IP address for outgoing traffic.&lt;/li>
&lt;li>IP pool(s) for the &lt;strong>load balancers&lt;/strong>
The IP pool name(s) need to be specified in the cloud profile at &lt;code>spec.providerConfig.contraints.loadBalancerConfig.classes[*].ipPoolName&lt;/code>. An IP address is needed for every port of every Kubernetes service of type &lt;code>LoadBalancer&lt;/code>.&lt;/li>
&lt;/ol>
&lt;p>To create them, follow these steps in the NSX-T Manager UI in the web browser:&lt;/p>
&lt;ol>
&lt;li>From the &lt;em>toolbar&lt;/em> at the top of the page choose &lt;em>Networking&lt;/em>&lt;/li>
&lt;li>From the left side list choose &lt;em>IP Address Pools&lt;/em> below the &lt;em>IP Management&lt;/em>&lt;/li>
&lt;li>Press the &lt;em>ADD IP ADRESS POOL&lt;/em> button&lt;/li>
&lt;li>Enter &lt;em>Name&lt;/em>&lt;/li>
&lt;li>Enter at least one subnet by clicking on &lt;em>Sets&lt;/em>&lt;/li>
&lt;li>Press the &lt;em>Save&lt;/em> button&lt;/li>
&lt;/ol>
&lt;h4 id="sizing-the-ip-pools">Sizing the IP pools&lt;/h4>
&lt;p>Each shoot cluster needs one IP address for SNAT and at least two IP addresses for load balancers VIPs (kube-apiservcer and Gardener shoot-seed VPN). A third IP address may be needed for ingress.
Depending on the payload of a shoot cluster, there may be additional services of type &lt;code>LoadBalancer&lt;/code>. An IP address is needed for every port of every Kubernetes service of type &lt;code>LoadBalancer&lt;/code>.&lt;/p>
&lt;h3 id="check-edge-cluster-sizing">Check edge cluster sizing&lt;/h3>
&lt;p>For load balancer related configurations limitations of NSX-T, please see the web pages &lt;a href="https://configmax.vmware.com/guest?vmwareproduct=NSX-T%20Data%20Center&amp;amp;release=NSX-T%20Data%20Center%203.1.0&amp;amp;categories=20-0">VMware Configuration Maximums&lt;/a>. The link shows the limitations for NSX-T 3.1, if you have another version, please select the version from the left panel under &lt;em>Select Version&lt;/em> and press the &lt;em>VIEW LIMITS&lt;/em> button to update the view.&lt;/p>
&lt;p>By default settings, each shoot cluster has an own T1 gateway and an own LB service (instance) of &amp;ldquo;T-shirt&amp;rdquo; size &lt;code>SMALL&lt;/code>.&lt;/p>
&lt;p>Examples for limitations on NSX-T 3.1 using &lt;em>Large Edge Node&lt;/em> and &lt;em>SMALL&lt;/em> load balancers instances:&lt;/p>
&lt;ol>
&lt;li>
&lt;p>There is a limit of 40 small LB instances per egde cluster (for HA 40 per pair of edge nodes)&lt;/p>
&lt;p>=&amp;gt; maximum number of shoot clusters = 40 * (number of edge nodes) / 2&lt;/p>
&lt;/li>
&lt;li>
&lt;p>For &lt;code>SMALL&lt;/code> load balancers, there is a maximum of 20 virtual servers. A virtual server is needed for every port of a service of type &lt;code>LoadBalancer&lt;/code>&lt;/p>
&lt;p>=&amp;gt; maximum number of services/ports pairs = 20 * (number of edge nodes) / 2&lt;/p>
&lt;p>The load balancer &amp;ldquo;T-shirt&amp;rdquo; size can be set on cloud profile level (&lt;code>spec.providerConfig.contraints.loadBalancerConfig.size&lt;/code>) or in the shoot manifest (&lt;code>spec.provider.controlPlaneConfig.loadBalancerSize&lt;/code>)&lt;/p>
&lt;/li>
&lt;li>
&lt;p>The number of pool members is limited to 7,500. For every K8s service port, every worker node is a pool member.&lt;/p>
&lt;p>=&amp;gt; If every shoot cluster has an average number of 15 worker nodes, there can be 500 service/port pairs over all shoot clusters per pair of edge nodes&lt;/p>
&lt;/li>
&lt;/ol>
&lt;h2 id="get-vds-uuids">Get VDS UUIDs&lt;/h2>
&lt;p>This step is only needed, if there are several VDS (virtual distributed switches) for each zone.&lt;/p>
&lt;p>In this case, their UUIDs need to be fetched and set in the cloud profile at &lt;code>spec.providerConfig.regions[*].zones[*].switchUuid&lt;/code>.&lt;/p>
&lt;p>Unfortunately, they are not displayed in the vSphere Client.&lt;/p>
&lt;p>Here the command line tool &lt;code>govc&lt;/code> is used to look them
up.&lt;/p>
&lt;ol>
&lt;li>Run &lt;code>govc find / -type DistributedVirtualSwitch&lt;/code> to get the full path of all vds/dvs&lt;/li>
&lt;li>For each switch run &lt;code>govc dvs.portgroup.info &amp;lt;switch-path&amp;gt; | grep DvsUuid&lt;/code>&lt;/li>
&lt;/ol></description></item><item><title>Docs: Tanzu Vsphere</title><link>https://gardener.cloud/docs/extensions/infrastructure-extensions/gardener-extension-provider-vsphere/docs/tanzu-vsphere/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://gardener.cloud/docs/extensions/infrastructure-extensions/gardener-extension-provider-vsphere/docs/tanzu-vsphere/</guid><description>
&lt;h2 id="create-tanzu-cluster">Create Tanzu Cluster&lt;/h2>
&lt;p>For gardener a Tanzu Kubernetes „guest” cluster is used. Look here for the vSphere documentation &lt;a href="https://docs.vmware.com/en/VMware-vSphere/7.0/vmware-vsphere-with-tanzu/GUID-2597788E-2FA4-420E-B9BA-9423F8F7FD9F.html">Provisioning Tanzu Kubernetes Clusters&lt;/a>&lt;/p>
&lt;h3 id="virtual-machine-classes">Virtual Machine Classes&lt;/h3>
&lt;p>For gardener the minimum Virtual Machine Classes must set to &lt;code>best-effort-large&lt;/code>.&lt;/p>
&lt;h3 id="network-settings">Network Settings&lt;/h3>
&lt;p>For the deployment it is possible to provision the cluster with a minimal amount of configuration parameter. It is recommended to set the parameter &lt;code>Default Pod CIDR&lt;/code>, &lt;code>Default Services CIDR&lt;/code> with values which fit to your enviroment.&lt;/p>
&lt;h3 id="storage-class-settings">Storage Class settings&lt;/h3>
&lt;p>The &lt;code>storageClass&lt;/code> Parameter should be defined to avoid problems during deployment.&lt;/p>
&lt;p>Example:&lt;/p>
&lt;pre>&lt;code>```yaml
apiVersion: run.tanzu.vmware.com/v1alpha1 #TKG API endpoint
kind: TanzuKubernetesCluster #required parameter
metadata:
name: tkg-cluster-1 #cluster name, user defined
namespace: ns1 #supervisor namespace
spec:
distribution:
version: v1.17 #resolved kubernetes version
topology:
controlPlane:
count: 1 #number of control plane nodes
class: best-effort-small #vmclass for control plane nodes
storageClass: vsan-default-storage-policy #storageclass for control plane
workers:
count: 3 #number of worker nodes
class: best-effort-large #vmclass for worker nodes
storageClass: vsan-default-storage-policy #storageclass for worker nodes
settings:
network:
cni:
name: calico
services:
cidrBlocks: [&amp;quot;198.51.100.0/12&amp;quot;] #Cannot overlap with Supervisor Cluster
pods:
cidrBlocks: [&amp;quot;192.0.2.0/16&amp;quot;] #Cannot overlap with Supervisor Cluster
```
&lt;/code>&lt;/pre></description></item><item><title>Docs: Usage As End User</title><link>https://gardener.cloud/docs/extensions/infrastructure-extensions/gardener-extension-provider-vsphere/docs/usage-as-end-user/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://gardener.cloud/docs/extensions/infrastructure-extensions/gardener-extension-provider-vsphere/docs/usage-as-end-user/</guid><description>
&lt;h1 id="using-the-vsphere-provider-extension-with-gardener-as-end-user">Using the vSphere provider extension with Gardener as end-user&lt;/h1>
&lt;p>The &lt;a href="https://github.com/gardener/gardener/blob/master/example/90-shoot.yaml">&lt;code>core.gardener.cloud/v1alpha1.Shoot&lt;/code> resource&lt;/a> declares a few fields that are meant to contain provider-specific configuration.&lt;/p>
&lt;p>In this document we are describing how this configuration looks like for VMware vSphere and provide an example &lt;code>Shoot&lt;/code> manifest with minimal configuration that you can use to create an vSphere cluster (modulo the landscape-specific information like cloud profile names, secret binding names, etc.).&lt;/p>
&lt;h2 id="provider-secret-data">Provider secret data&lt;/h2>
&lt;p>Every shoot cluster references a &lt;code>SecretBinding&lt;/code> which itself references a &lt;code>Secret&lt;/code>, and this &lt;code>Secret&lt;/code> contains the provider credentials of your vSphere tenant.
It contains two authentication sets. One for the vSphere host and another for the NSX-T host, which is needed to set up the network infrastructure.
This &lt;code>Secret&lt;/code> must look as follows:&lt;/p>
&lt;div class="highlight">&lt;pre style="background-color:#fff;-moz-tab-size:4;-o-tab-size:4;tab-size:4">&lt;code class="language-yaml" data-lang="yaml">apiVersion: v1
kind: Secret
metadata:
name: core-vsphere
namespace: garden-dev
type: Opaque
data:
vspherePassword: base64(vsphere-password)
vsphereInsecureSSL: base64(&amp;#34;true&amp;#34;|&amp;#34;false&amp;#34;)
nsxtPassword: base64(NSX-T-password)
nsxtInsecureSSL: base64(&amp;#34;true&amp;#34;|&amp;#34;false&amp;#34;)
&lt;/code>&lt;/pre>&lt;/div>&lt;p>Here &lt;code>base64(...)&lt;/code> are only a placeholders for the Base64 encoded values.&lt;/p>
&lt;h2 id="infrastructureconfig">&lt;code>InfrastructureConfig&lt;/code>&lt;/h2>
&lt;p>The infrastructure configuration is used for advanced scenarios only.
Nodes on all zones are using IP addresses from the common nodes network as the network is managed by NSX-T.
The infrastructure controller will create several network objects using NSX-T. A network segment is used as the subnet
for the VMs (nodes), a tier-1 gateway, a DHCP server, and a SNAT for the nodes.&lt;/p>
&lt;p>An example &lt;code>InfrastructureConfig&lt;/code> for the vSphere extension looks as follows.
You only need to specify it, if you either want to use an existing Tier-1 gateway and load balancer service pair
or if you want to overwrite the automatic selection of the NSX-T version.&lt;/p>
&lt;div class="highlight">&lt;pre style="background-color:#fff;-moz-tab-size:4;-o-tab-size:4;tab-size:4">&lt;code class="language-yaml" data-lang="yaml">infrastructureConfig:
apiVersion: vsphere.provider.extensions.gardener.cloud/v1alpha1
kind: InfrastructureConfig
&lt;span style="color:#008000">#overwriteNSXTInfraVersion: &amp;#39;1&amp;#39;&lt;/span>
&lt;span style="color:#008000">#networks:&lt;/span>
&lt;span style="color:#008000"># tier1GatewayPath: /infra/tier-1s/tier1gw-b8213651-9659-4180-8bfd-1e16228e8dcb&lt;/span>
&lt;span style="color:#008000"># loadBalancerServicePath: /infra/lb-services/708c5cb1-e5d0-4b16-906f-ec7177a1485d&lt;/span>
&lt;/code>&lt;/pre>&lt;/div>&lt;h3 id="advanced-configuration-settings">Advanced configuration settings&lt;/h3>
&lt;h4 id="section-networks">Section networks&lt;/h4>
&lt;p>By default, the infrastructure controller creates a separate Tier-1 gateway for each shoot cluster
and the cloud controller manager (&lt;code>vsphere-cloud-provider&lt;/code>) creates a load balancer service.&lt;/p>
&lt;p>If an existing Tier-1 gateway should be used, you can specify its &amp;lsquo;path&amp;rsquo;. In this case, there
must also be a load balancer service defined for this tier-1 gateway and its &amp;lsquo;path&amp;rsquo; needs to be specified, too.
In the NSX-T manager UI, the path of the tier-1 gateway can be found at &lt;code>Networking / Tier-1 Gateways&lt;/code>.
Then select &lt;code>Copy path to clipboard&lt;/code> from the context menu of the tier-1 gateway
(click on the three vertical dots on the left of the row). Do the same with the
corresponding load balancer at &lt;code>Networking / Load balancing / Tab Load Balancers&lt;/code>
For security reasons the referenced Tier-1 gateway in NSX-T must have a tag with scope &lt;code>authorized-shoots&lt;/code> and its
tag value consists of a comma-separated list of the allowed shoot names in the format &lt;code>shoot--&amp;lt;project&amp;gt;--&amp;lt;name&amp;gt;&lt;/code>
(optionally with wildcard &lt;code>*&lt;/code>). Additionally, it must have a tag with scope &lt;code>garden&lt;/code> set to the garden ID.&lt;/p>
&lt;p>Example:&lt;/p>
&lt;div class="highlight">&lt;pre style="background-color:#fff;-moz-tab-size:4;-o-tab-size:4;tab-size:4">&lt;code class="language-yaml" data-lang="yaml">infrastructureConfig:
apiVersion: vsphere.provider.extensions.gardener.cloud/v1alpha1
kind: InfrastructureConfig
networks:
tier1GatewayPath: /infra/tier-1s/tier1gw-b8213651-9659-4180-8bfd-1e16228e8dcb
loadBalancerServicePath: /infra/lb-services/708c5cb1-e5d0-4b16-906f-ec7177a1485d
&lt;/code>&lt;/pre>&lt;/div>&lt;p>Please ensure, that the worker nodes cidr (shoot manifest &lt;code>spec.networking.nodes&lt;/code>) do not overlap with
other existing segments of the selected tier-1 gateway.&lt;/p>
&lt;h4 id="option-overwritensxtinfraversion">Option overwriteNSXTInfraVersion&lt;/h4>
&lt;p>The option &lt;code>overwriteNSXTInfraVersion&lt;/code> can be used to change the network objects created during the initial infrastructure creation.
By default the infra-version is automatically selected according to the NSX-T version. The infra-version &lt;code>'1'&lt;/code> is used
for NSX-T 2.5, and infra-version &lt;code>'2'&lt;/code> for NSX-T versions &amp;gt;= 3.0. The difference is creation of the the logical DHCP server.
For NSX-T 2.5, only the DHCP server of the &amp;ldquo;Advanced API&amp;rdquo; is usable. For NSX-T &amp;gt;= 3.0 the new DHCP server is default,
but for special purposes infra-version &lt;code>'1'&lt;/code> is also allowed.&lt;/p>
&lt;h2 id="controlplaneconfig">&lt;code>ControlPlaneConfig&lt;/code>&lt;/h2>
&lt;p>The control plane configuration mainly contains values for the vSphere-specific control plane components.
Today, the only component deployed by the vSphere extension is the &lt;code>cloud-controller-manager&lt;/code>.&lt;/p>
&lt;p>An example &lt;code>ControlPlaneConfig&lt;/code> for the vSphere extension looks as follows:&lt;/p>
&lt;div class="highlight">&lt;pre style="background-color:#fff;-moz-tab-size:4;-o-tab-size:4;tab-size:4">&lt;code class="language-yaml" data-lang="yaml">apiVersion: vsphere.provider.extensions.gardener.cloud/v1alpha1
kind: ControlPlaneConfig
loadBalancerClasses:
- name: mypubliclbclass
- name: myprivatelbclass
ipPoolName: pool42 &lt;span style="color:#008000"># optional overwrite&lt;/span>
loadBalancerSize: SMALL
cloudControllerManager:
featureGates:
CustomResourceValidation: &lt;span style="color:#00f">true&lt;/span>
&lt;/code>&lt;/pre>&lt;/div>&lt;p>The &lt;code>loadBalancerClasses&lt;/code> optionally defines the load balancer classes to be used.
The specified names must be defined in the constraints section of the cloud profile.
If the list contains a load balancer named &amp;ldquo;default&amp;rdquo;, it is used as the default load balancer.
Otherwise the first one is also the default.
If no classes are specified the default load balancer class is used as defined in the cloud profile constraints section.
If the ipPoolName is overwritten, the corresponding IP pool object in NSX-T must have a tag with scope &lt;code>authorized-shoots&lt;/code> and its
tag value consists of a comma-separated list of the allowed shoot names in the format &lt;code>shoot--&amp;lt;project&amp;gt;--&amp;lt;name&amp;gt;&lt;/code>
(optionally with wildcard &lt;code>*&lt;/code>). Additionally, it must have a tag with scope &lt;code>garden&lt;/code> set to the garden ID.&lt;/p>
&lt;p>The &lt;code>loadBalancerSize&lt;/code> is optional and overwrites the default value specified in the cloud profile config.
It must be one of the values &lt;code>SMALL&lt;/code>, &lt;code>MEDIUM&lt;/code>, or &lt;code>LARGE&lt;/code>. &lt;code>SMALL&lt;/code> can manage 10 service ports,
&lt;code>MEDIUM&lt;/code> 100, and &lt;code>LARGE&lt;/code> 1000.&lt;/p>
&lt;p>The &lt;code>cloudControllerManager.featureGates&lt;/code> contains an optional map of explicitly enabled or disabled feature gates.
For production usage it&amp;rsquo;s not recommend to use this field at all as you can enable alpha features or disable beta/stable features, potentially impacting the cluster stability.
If you don&amp;rsquo;t want to configure anything for the &lt;code>cloudControllerManager&lt;/code> simply omit the key in the YAML specification.&lt;/p>
&lt;h2 id="example-shoot-manifest-one-availability-zone">Example &lt;code>Shoot&lt;/code> manifest (one availability zone)&lt;/h2>
&lt;p>Please find below an example &lt;code>Shoot&lt;/code> manifest for one availability zone:&lt;/p>
&lt;div class="highlight">&lt;pre style="background-color:#fff;-moz-tab-size:4;-o-tab-size:4;tab-size:4">&lt;code class="language-yaml" data-lang="yaml">apiVersion: core.gardener.cloud/v1alpha1
kind: Shoot
metadata:
name: johndoe-vsphere
namespace: garden-dev
spec:
cloudProfileName: vsphere
region: europe-1
secretBindingName: core-vsphere
provider:
type: vsphere
&lt;span style="color:#008000">#infrastructureConfig:&lt;/span>
&lt;span style="color:#008000"># apiVersion: vsphere.provider.extensions.gardener.cloud/v1alpha1&lt;/span>
&lt;span style="color:#008000"># kind: InfrastructureConfig&lt;/span>
&lt;span style="color:#008000"># overwriteNSXTInfraVersion: &amp;#39;1&amp;#39;&lt;/span>
controlPlaneConfig:
apiVersion: vsphere.provider.extensions.gardener.cloud/v1alpha1
kind: ControlPlaneConfig
&lt;span style="color:#008000"># loadBalancerClasses:&lt;/span>
&lt;span style="color:#008000"># - name: mylbclass&lt;/span>
workers:
- name: worker-xoluy
machine:
type: std-04
minimum: 2
maximum: 2
zones:
- europe-1a
networking:
nodes: 10.250.0.0/16
type: calico
kubernetes:
version: 1.16.1
maintenance:
autoUpdate:
kubernetesVersion: &lt;span style="color:#00f">true&lt;/span>
machineImageVersion: &lt;span style="color:#00f">true&lt;/span>
addons:
kubernetes-dashboard:
enabled: &lt;span style="color:#00f">true&lt;/span>
nginx-ingress:
enabled: &lt;span style="color:#00f">true&lt;/span>
&lt;/code>&lt;/pre>&lt;/div>&lt;h2 id="kubernetes-versions-per-worker-pool">Kubernetes Versions per Worker Pool&lt;/h2>
&lt;p>This extension supports &lt;code>gardener/gardener&lt;/code>&amp;rsquo;s &lt;code>WorkerPoolKubernetesVersion&lt;/code> feature gate, i.e., having &lt;a href="https://github.com/gardener/gardener/blob/8a9c88866ec5fce59b5acf57d4227eeeb73669d7/example/90-shoot.yaml#L69-L70">worker pools with overridden Kubernetes versions&lt;/a> since &lt;code>gardener-extension-provider-vsphere@v0.12&lt;/code>.&lt;/p></description></item><item><title>Docs: Usage As Operator</title><link>https://gardener.cloud/docs/extensions/infrastructure-extensions/gardener-extension-provider-vsphere/docs/usage-as-operator/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://gardener.cloud/docs/extensions/infrastructure-extensions/gardener-extension-provider-vsphere/docs/usage-as-operator/</guid><description>
&lt;h1 id="using-the-vsphere-provider-extension-with-gardener-as-operator">Using the vSphere provider extension with Gardener as operator&lt;/h1>
&lt;p>The &lt;a href="https://github.com/gardener/gardener/blob/master/example/30-cloudprofile.yaml">&lt;code>core.gardener.cloud/v1alpha1.CloudProfile&lt;/code> resource&lt;/a> declares a &lt;code>providerConfig&lt;/code> field that is meant to contain provider-specific configuration.&lt;/p>
&lt;p>In this document we are describing how this configuration looks like for VMware vSphere and provide an example &lt;code>CloudProfile&lt;/code> manifest with minimal configuration that you can use to allow creating vSphere shoot clusters.&lt;/p>
&lt;h2 id="cloudprofileconfig">&lt;code>CloudProfileConfig&lt;/code>&lt;/h2>
&lt;p>The cloud profile configuration contains information about the real machine image paths in the vSphere environment (image names).
You have to map every version that you specify in &lt;code>.spec.machineImages[].versions&lt;/code> here such that the vSphere extension knows the image ID for every version you want to offer.&lt;/p>
&lt;p>It also contains optional default values for DNS servers that shall be used for shoots.
In the &lt;code>dnsServers[]&lt;/code> list you can specify IP addresses that are used as DNS configuration for created shoot subnets.&lt;/p>
&lt;p>The &lt;code>dhcpOptions&lt;/code> list allows to specify DHCP options. See &lt;a href="https://www.iana.org/assignments/bootp-dhcp-parameters/bootp-dhcp-parameters.xhtml">BOOTP Vendor Extensions and DHCP Options&lt;/a>
for valid codes (tags) and details about values. The code &lt;code>15&lt;/code> (domain name) is only allowed for
when using NSX-T 2.5. For NSX-T &amp;gt;= 3.0 use &lt;code>119&lt;/code> (search domain).&lt;/p>
&lt;p>The &lt;code>dockerDaemonOptions&lt;/code> allow to adjust the docker daemon configuration.&lt;/p>
&lt;ul>
&lt;li>with &lt;code>dockerDaemonOptions.httpProxyConf&lt;/code> the content of the proxy configuration file can be set.
See &lt;a href="https://docs.docker.com/config/daemon/systemd/#httphttps-proxy">Docker HTTP/HTTPS proxy&lt;/a> for more details&lt;/li>
&lt;li>with &lt;code>dockerDaemonOptions.insecureRegistries&lt;/code> insecure registries can be specified. This
should only be used for development or evaluation purposes.&lt;/li>
&lt;/ul>
&lt;p>Also, you have to specify several name of NSX-T objects in the constraints.&lt;/p>
&lt;p>An example &lt;code>CloudProfileConfig&lt;/code> for the vSphere extension looks as follows:&lt;/p>
&lt;div class="highlight">&lt;pre style="background-color:#fff;-moz-tab-size:4;-o-tab-size:4;tab-size:4">&lt;code class="language-yaml" data-lang="yaml">apiVersion: vsphere.provider.extensions.gardener.cloud/v1alpha1
kind: CloudProfileConfig
namePrefix: my_gardener
defaultClassStoragePolicyName: &lt;span style="color:#a31515">&amp;#34;vSAN Default Storage Policy&amp;#34;&lt;/span>
folder: my-vsphere-vm-folder
regions:
- name: region1
vsphereHost: my.vsphere.host
vsphereInsecureSSL: &lt;span style="color:#00f">true&lt;/span>
nsxtHost: my.vsphere.host
nsxtInsecureSSL: &lt;span style="color:#00f">true&lt;/span>
transportZone: &lt;span style="color:#a31515">&amp;#34;my-tz&amp;#34;&lt;/span>
logicalTier0Router: &lt;span style="color:#a31515">&amp;#34;my-tier0router&amp;#34;&lt;/span>
edgeCluster: &lt;span style="color:#a31515">&amp;#34;my-edgecluster&amp;#34;&lt;/span>
snatIpPool: &lt;span style="color:#a31515">&amp;#34;my-snat-ip-pool&amp;#34;&lt;/span>
datacenter: my-vsphere-dc
zones:
- name: zone1
computeCluster: my-vsphere-computecluster1
&lt;span style="color:#008000"># resourcePool: my-resource-pool1 # provide either computeCluster or resourcePool or hostSystem&lt;/span>
&lt;span style="color:#008000"># hostSystem: my-host1 # provide either computeCluster or resourcePool or hostSystem&lt;/span>
datastore: my-vsphere-datastore1
&lt;span style="color:#008000">#datastoreCluster: my-vsphere-datastore-cluster # provide either datastore or datastoreCluster&lt;/span>
- name: zone2
computeCluster: my-vsphere-computecluster2
&lt;span style="color:#008000"># resourcePool: my-resource-pool2 # provide either computeCluster or resourcePool or hostSystem&lt;/span>
&lt;span style="color:#008000"># hostSystem: my-host2 # provide either computeCluster or resourcePool or hostSystem&lt;/span>
datastore: my-vsphere-datastore2
&lt;span style="color:#008000">#datastoreCluster: my-vsphere-datastore-cluster # provide either datastore or datastoreCluster&lt;/span>
constraints:
loadBalancerConfig:
size: MEDIUM
classes:
- name: default
ipPoolName: gardener_lb_vip
&lt;span style="color:#008000"># optional DHCP options like 119 (search domain), 42 (NTP), 15 (domain name (only NSX-T 2.5))&lt;/span>
&lt;span style="color:#008000">#dhcpOptions:&lt;/span>
&lt;span style="color:#008000">#- code: 15&lt;/span>
&lt;span style="color:#008000"># values:&lt;/span>
&lt;span style="color:#008000"># - foo.bar.com&lt;/span>
&lt;span style="color:#008000">#- code: 42&lt;/span>
&lt;span style="color:#008000"># values:&lt;/span>
&lt;span style="color:#008000"># - 136.243.202.118&lt;/span>
&lt;span style="color:#008000"># - 80.240.29.124&lt;/span>
&lt;span style="color:#008000"># - 78.46.53.8&lt;/span>
&lt;span style="color:#008000"># - 162.159.200.123&lt;/span>
dnsServers:
- 10.10.10.11
- 10.10.10.12
machineImages:
- name: coreos
versions:
- version: 2191.5.0
path: gardener/templates/coreos-2191.5.0
guestId: coreos64Guest
&lt;span style="color:#008000">#dockerDaemonOptions:&lt;/span>
&lt;span style="color:#008000"># httpProxyConf: |&lt;/span>
&lt;span style="color:#008000"># [Service]&lt;/span>
&lt;span style="color:#008000"># Environment=&amp;#34;HTTPS_PROXY=https://proxy.example.com:443&amp;#34;&lt;/span>
&lt;span style="color:#008000"># insecureRegistries:&lt;/span>
&lt;span style="color:#008000"># - myregistrydomain.com:5000&lt;/span>
&lt;span style="color:#008000"># - blabla.mycompany.local&lt;/span>
&lt;/code>&lt;/pre>&lt;/div>&lt;h2 id="example-cloudprofile-manifest">Example &lt;code>CloudProfile&lt;/code> manifest&lt;/h2>
&lt;p>Please find below an example &lt;code>CloudProfile&lt;/code> manifest:&lt;/p>
&lt;div class="highlight">&lt;pre style="background-color:#fff;-moz-tab-size:4;-o-tab-size:4;tab-size:4">&lt;code class="language-yaml" data-lang="yaml">apiVersion: core.gardener.cloud/v1beta1
kind: CloudProfile
metadata:
name: vsphere
spec:
type: vsphere
providerConfig:
apiVersion: vsphere.provider.extensions.gardener.cloud/v1alpha1
kind: CloudProfileConfig
namePrefix: my_gardener
defaultClassStoragePolicyName: &lt;span style="color:#a31515">&amp;#34;vSAN Default Storage Policy&amp;#34;&lt;/span>
folder: my-vsphere-vm-folder
regions:
- name: region1
vsphereHost: my.vsphere.host
vsphereInsecureSSL: &lt;span style="color:#00f">true&lt;/span>
nsxtHost: my.vsphere.host
nsxtInsecureSSL: &lt;span style="color:#00f">true&lt;/span>
transportZone: &lt;span style="color:#a31515">&amp;#34;my-tz&amp;#34;&lt;/span>
logicalTier0Router: &lt;span style="color:#a31515">&amp;#34;my-tier0router&amp;#34;&lt;/span>
edgeCluster: &lt;span style="color:#a31515">&amp;#34;my-edgecluster&amp;#34;&lt;/span>
snatIpPool: &lt;span style="color:#a31515">&amp;#34;my-snat-ip-pool&amp;#34;&lt;/span>
datacenter: my-vsphere-dc
zones:
- name: zone1
computeCluster: my-vsphere-computecluster1
&lt;span style="color:#008000"># resourcePool: my-resource-pool1 # provide either computeCluster or resourcePool or hostSystem&lt;/span>
&lt;span style="color:#008000"># hostSystem: my-host1 # provide either computeCluster or resourcePool or hostSystem&lt;/span>
datastore: my-vsphere-datastore1
&lt;span style="color:#008000">#datastoreCluster: my-vsphere-datastore-cluster # provide either datastore or datastoreCluster&lt;/span>
- name: zone2
computeCluster: my-vsphere-computecluster2
&lt;span style="color:#008000"># resourcePool: my-resource-pool2 # provide either computeCluster or resourcePool or hostSystem&lt;/span>
&lt;span style="color:#008000"># hostSystem: my-host2 # provide either computeCluster or resourcePool or hostSystem&lt;/span>
datastore: my-vsphere-datastore2
&lt;span style="color:#008000">#datastoreCluster: my-vsphere-datastore-cluster # provide either datastore or datastoreCluster&lt;/span>
constraints:
loadBalancerConfig:
size: MEDIUM
classes:
- name: default
ipPoolName: gardener_lb_vip
dnsServers:
- 10.10.10.11
- 10.10.10.12
machineImages:
- name: coreos
versions:
- version: 2191.5.0
path: gardener/templates/coreos-2191.5.0
guestId: coreos64Guest
kubernetes:
versions:
- version: 1.15.4
- version: 1.16.0
- version: 1.16.1
machineImages:
- name: coreos
versions:
- version: 2191.5.0
machineTypes:
- name: std-02
cpu: &lt;span style="color:#a31515">&amp;#34;2&amp;#34;&lt;/span>
gpu: &lt;span style="color:#a31515">&amp;#34;0&amp;#34;&lt;/span>
memory: 8Gi
usable: &lt;span style="color:#00f">true&lt;/span>
- name: std-04
cpu: &lt;span style="color:#a31515">&amp;#34;4&amp;#34;&lt;/span>
gpu: &lt;span style="color:#a31515">&amp;#34;0&amp;#34;&lt;/span>
memory: 16Gi
usable: &lt;span style="color:#00f">true&lt;/span>
- name: std-08
cpu: &lt;span style="color:#a31515">&amp;#34;8&amp;#34;&lt;/span>
gpu: &lt;span style="color:#a31515">&amp;#34;0&amp;#34;&lt;/span>
memory: 32Gi
usable: &lt;span style="color:#00f">true&lt;/span>
regions:
- name: region1
zones:
- name: zone1
- name: zone2
&lt;/code>&lt;/pre>&lt;/div>&lt;h2 id="which-versions-of-kubernetesvsphere-are-supported">Which versions of Kubernetes/vSphere are supported&lt;/h2>
&lt;p>This extension targets Kubernetes &amp;gt;= &lt;code>v1.15&lt;/code> and vSphere &lt;code>6.7 U3&lt;/code> or later.&lt;/p>
&lt;ul>
&lt;li>vSphere CSI driver needs vSphere &lt;code>6.7 U3&lt;/code> or later,
and Kubernetes &amp;gt;= &lt;code>v1.14&lt;/code>
(see &lt;a href="https://docs.vmware.com/en/VMware-vSphere-Container-Storage-Plug-in/2.0/vmware-vsphere-csp-getting-started/GUID-E59B13F5-6F49-4619-9877-DF710C365A1E.html">feature metrics&lt;/a> for more details)&lt;/li>
&lt;li>vSpere CPI driver needs vSphere &lt;code>6.7 U3&lt;/code> or later,
and Kubernetes &amp;gt;= &lt;code>v1.11&lt;/code>
(see &lt;a href="https://github.com/kubernetes/cloud-provider-vsphere/blob/master/docs/book/cloud_provider_interface.md#which-versions-of-kubernetesvsphere-support-it">cloud-provider-vsphere CPI - Cloud Provider Interface&lt;/a>)&lt;/li>
&lt;/ul>
&lt;h2 id="supported-vm-images">Supported VM images&lt;/h2>
&lt;p>Currently, only CoreOS and Flatcar (CoreOS fork) are supported.
Virtual Machine Hardware must be version 15 or higher, but images are upgraded
automatically if their hardware has an older version.&lt;/p></description></item></channel></rss>